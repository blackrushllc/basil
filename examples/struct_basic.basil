REM Basic structure (TYPE) demo
REM NOTE: Requires TYPE ... END TYPE support; the current interpreter build may not yet support this.

TYPE Person
  DIM Name$ AS STRING * 16
  DIM Age%
END TYPE

DIM p AS Person
p.Name$ = "Ada"
p.Age% = 36
PRINT "Person: " + p.Name$ + " (Age " + p.Age% + ")"
